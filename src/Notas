Con hooks puedo encapsular la lógica de llamada a la API para que se llame un método en
el componentDidMount de cada página y no se tenga que implementar en cada una.

Pendiente:
Mirar porqué se están guardando dos objetos indefinidos cuando se llama
al API